<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiabetesTreatmentCenter">

        <!-- Main/Role Selection Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity android:name=".AuthChoiceActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- Patient Activities -->
        <activity android:name=".PatientDashboardActivity" android:exported="false" />
        <activity android:name=".PredictionActivity" android:exported="false" />
        <activity android:name=".DoctorsListActivity" android:exported="false" />
        <activity android:name=".BookAppointmentActivity" android:exported="false" />
        <activity android:name=".PatientAppointmentsActivity" android:exported="false" />
        <activity android:name=".HealthHistoryActivity" android:exported="false" />
        <activity android:name=".PredictionHistoryActivity" android:exported="false" />

        <!-- Doctor Activities -->
        <activity android:name=".DoctorDashboardActivity" android:exported="false" />
        <activity android:name=".DoctorAppointmentsActivity" android:exported="false" />

        <!-- FileProvider for PDF sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>